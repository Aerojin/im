import React from "react";
import { Button, Popover } from "antd";
import "./index.css";

const Header = (props) => {
  const { children } = props || {};

  console.log(111);

  return (
  <div >
    header
  </div>
  );
};

export default Header;
