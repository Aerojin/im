import React from "react";
import { Button, Popover } from "antd";
import "./index.css";

const Footer = (props) => {
  const { children } = props || {};

  console.log(111);

  return (
  <div>
    footer
  </div>
  );
};

export default Footer;
