import React from "react";
import { Button, Popover } from "antd";
import "./index.css";

const Content = (props) => {
  const { children } = props || {};

  console.log(111);

  return (
  <div >
    content
  </div>
  );
};

export default Content;
